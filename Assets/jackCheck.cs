using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class jackCheck : MonoBehaviour
{
    public bool inLocation;
    public bool raised;
    public bool lowered;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
